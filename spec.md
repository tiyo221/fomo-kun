# spec.md — FOMO解消ツール「fomo-kun」

## 1. 目的

使用者の **FOMO（Fear of Missing Out / 見逃すことへの不安）** に対して、

1. 今の落ち着かなさが **FOMOなのか／そうでないのか を自覚** させ、
2. その不安を **解消（手放す）** 方向へ導く

ことを目的とした、内省・セルフケア型のツール。

**やらないこと**：FOMOを煽らない。他人の充実や「みんなが見ている」情報を見せない。数値や連続記録で競わせない。プレッシャー（義務感）を生まない。

## 2. 基本方針・制約

| 項目 | 内容 |
|---|---|
| 形態 | **単一HTMLファイル**のみで動作 |
| 外部依存 | **一切なし**（CDN・フォント・API・画像URL等を使わない。CSS/JSはインライン） |
| ネットワーク | 通信を行わない（オフラインで完全動作） |
| データ保存 | `localStorage` のみ。ユーザーが任意で**全消去**できる |
| 対象ブラウザ | モダンブラウザ（Chrome / Edge / Safari / Firefox 最新） |
| 利用シーン | **即効型**（ソワソワした瞬間に開いて鎮める）をコアに、振り返りログを軽く付随 |
| トーン | 瞑想的・静か。落ち着いた配色、余白多め、急かさない |

## 3. 体験フロー

```
[1] 入口画面
      ↓
[2] 自覚ステップ（A/B切り分け）
      ├─ A: 具体的に何かを見逃している気がする   → [3A]
      └─ B: 理由はないけど何となく落ち着かない    → [3B]
      ↓
[3A] FOMOワーク
      - 気になっていることを1行書き出す
      - 問い返し（認知再構成）「それは1週間後も重要？」
      - 「見逃してOK」ボタンで手放す
      ↓
[3B] 切り分け結果の提示
      - 「これはFOMOではないかもしれません」
      - 落ち着かなさは疲れ・不安かもしれない、と伝える
      ↓
[4] 一言記録（任意）→ ログに保存
      ↓
[5] 締め画面（静かに終わる）

[*] 振り返り：入口からいつでも過去ログを閲覧／全消去できる
```

## 4. 画面・機能仕様

### 4.1 入口画面
- 静かなトーンで現在の状態を問う導入文を表示。
- ボタン：「はじめる」／「振り返りを見る」。

### 4.2 自覚ステップ（要求の核）
- 問い：「今のソワソワは、どちらに近い？」
- 選択肢：
  - **A**：具体的に何かを見逃している気がする
  - **B**：理由はないけど何となく落ち着かない
- 選んだ結果（A/B）はそのセッションのログに記録する。
- このラベリング行為自体が衝動を客観視させる狙い。

### 4.3 FOMOワーク（Aの場合）
1. **書き出し**：「何が気になっている？」を1行テキスト入力（任意・空でも進める）。
2. **認知再構成の問い返し**：入力後に静かに問いを提示。
   - 例：「それは1週間後も、同じくらい重要だと思う？」
   - 「はい／いいえ／わからない」程度の軽い応答、または読むだけでも可。
3. **手放す儀式**：「**見逃してOK**」ボタン。
   - 押すと、書き出した内容が静かにフェードアウトするなどの視覚的な「手放し」演出。
   - 「見逃しても、大丈夫」というメッセージを返す。

### 4.4 切り分け結果（Bの場合）
- 「これはFOMOではないかもしれません」と伝える。
- 落ち着かなさの正体（疲れ・漠然とした不安）に目を向ける短い文を提示。
- FOMOワークには進めず、そのまま締めへ向かえる。

### 4.5 一言記録（任意）
- セッションの最後に「今の気持ちを一言」入力欄（任意・スキップ可）。
- 保存対象：日時 / A・Bの別 / 気になっていたこと（任意）/ 一言（任意）。

### 4.6 振り返り
- 過去ログを新しい順に一覧表示。
- 「義務的な日課」にしない＝通知やストリーク表示はしない。あくまで気が向いたら見るもの。
- **全消去ボタン**：確認のうえ localStorage を空にできる。

## 5. データ仕様（localStorage）

- キー：`fomo-kun:logs`
- 値：JSON配列。1エントリの構造（案）：

```json
{
  "id": "string (一意ID)",
  "timestamp": "ISO 8601 文字列",
  "type": "A | B",
  "concern": "string | null   (気になっていたこと)",
  "note": "string | null      (締めの一言)"
}
```

- 保存・読込・全削除のみ。マイグレーションは当面不要（壊れた場合は安全に空配列へフォールバック）。

## 6. 非機能・設計上の配慮

- **悪化させない設計**：他者の情報・件数・「今○人が見ています」的な煽り表現を一切出さない。アニメーションは穏やかで短く、注意を奪わない。
- **プレッシャーを与えない**：すべてのワークはスキップ可能。「やらなければ」と思わせる文言を避ける。
- **プライバシー**：データは端末内のみ。いつでも全消去できることを明示。
- **アクセシビリティ**：十分なコントラスト、キーボード操作可、フォントは端末標準（system font stack）。
- **堅牢性**：localStorageが使えない環境でも、保存機能なしで動作継続（クラッシュさせない）。

## 7. スコープ外（将来検討）

- 呼吸ガイド、デジタル断ちタイマー、JOMOへの言い換えコンテンツ
- 通知・リマインダー、複数端末同期、エクスポート/インポート

## 8. 成果物

- `index.html`（単一ファイル。CSS/JSインライン、外部依存なし）
```

