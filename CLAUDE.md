# CLAUDE.md

このリポジトリで作業する際のガイドです。

## プロジェクト概要

使用者の FOMO（Fear of Missing Out / 見逃すことへの不安）を自覚・解消するためのセルフケアツール「fomo-kun」。
`index.html` をブラウザで開くだけで動作する。サーバー・ビルド・パッケージ管理・依存ライブラリは一切なし。

- 主要ファイル: [`index.html`](index.html) — HTML/CSS/JS をすべて内包した単一ファイル
- 仕様書: [`spec.md`](spec.md) — 目的・体験フロー・機能・データ仕様の定義
- データ永続化: ブラウザの `localStorage`（キー: `fomo-kun:logs`）。外部送信は行わない。
- 共有・バックアップ: 当面なし（データは端末内のみ。ユーザーが任意で全消去できる）。

目的（詳細は [`spec.md`](spec.md) 参照）:

- 今の落ち着かなさが **FOMOなのか／そうでないのか を自覚** させる
- その不安を **解消（手放す）** 方向へ導く

設計思想（重要）: **FOMOを悪化させない**。他者の情報・件数・「今○人が見ています」的な煽り表現を出さない。数値や連続記録で競わせない。すべてのワークはスキップ可能とし、義務感（新たなFOMO）を生まない。

## 構成

すべて `index.html` 一枚に収める方針。

- `<style>` — 全UIのCSS（CSS変数でテーマ管理）。瞑想的・静かなトーン、余白多め。
- `<script>` — セッションの状態を保持し、ログを localStorage へ保存する
  - ログ配列キー: `fomo-kun:logs`
  - 1エントリ: `{id, timestamp, type:"A"|"B", concern:string|null, note:string|null}`
    - `type` … 自覚ステップの選択（A=具体的に何かを見逃している気がする / B=理由はないけど何となく落ち着かない）
    - `concern` … 気になっていたこと（任意）
    - `note` … 締めの一言（任意）

### 体験フロー

1. 入口画面（静かなトーン。「はじめる」／「振り返りを見る」）
2. 自覚ステップ（A/B切り分け）— このラベリング自体が衝動の客観視を促す
3A. Aの場合: 書き出し → 認知再構成の問い返し（「それは1週間後も重要？」）→「見逃してOK」で手放す
3B. Bの場合: 「これはFOMOではないかもしれません」と切り分け結果を提示
4. 一言記録（任意）→ ログに保存
5. 締め画面（静かに終わる）
- 振り返り: 過去ログを新しい順に閲覧。**全消去**可能。通知・ストリーク表示はしない。

### 設計上の配慮

- **悪化させない**: 煽り・競争・他者比較を排除。アニメーションは穏やかで短く、注意を奪わない。
- **プレッシャーを与えない**: すべてスキップ可。「やらなければ」と思わせる文言を避ける。
- **プライバシー**: データは端末内のみ。いつでも全消去できることを明示。
- **堅牢性**: localStorageが使えない環境でも、保存なしで動作継続（クラッシュさせない。読み込み失敗時は空配列にフォールバック）。
- **アクセシビリティ**: 十分なコントラスト、キーボード操作可、system font stack。

## 動作確認

ビルド不要。`index.html` をブラウザで直接開くか、任意の静的サーバーで配信する。

```
npx --yes http-server -p 8777 -c-1
```

（`python` はこの環境ではWindowsストアのスタブで動かないため Node を使う。）

## 開発ワークフロー（必須ルール）

以下は**必ず守る**こと:

1. **作業は機能単位で Issue を立ててから始める。** 実装の前に GitHub Issue を作成し、何を作るか・受け入れ条件を明記する。
2. **作業はブランチを切って行い、PR を出す。** 直接 `main` にコミットしない。PR 本文に `Closes #<issue番号>` を入れて Issue と紐付ける。
3. **PR を出した後、必ず `/code-review` を実施する。** 指摘事項はマージ前に対応するか、対応しない場合は PR コメント or 別 Issue として残す。
4. **`/code-review` の指摘は次のサイクルで処理する。**
   1. 指摘内容を PR コメントとして残す（対応する／しないの方針も記載）。
   2. 対応すると判断した指摘を修正してコミット・プッシュする。
   3. 再度 `/code-review` を実施する。
   4. 新たな指摘がなくなるまで 1〜3 を繰り返す。問題がなければマージ確認に移る。

### 補足

- コミット・プッシュ・PR作成・マージは、ユーザーが明示的に指示したときのみ行う。
- コミットメッセージ・PR・Issue は日本語で記述する。
- 単一ファイル構成を崩さない（外部依存・ビルド工程を持ち込まない）ことを基本方針とする。
